# Studblocks

Roblox Studblocks plugin and local backend for converting selected block-part models into uploaded optimized mesh assets with stud textures.

## Layout

- `backend/` Python source, assets, and PyInstaller build script for the local backend app
- `plugin/` Roblox Studio plugin export (`Mesh To Part.rbxmx`)
